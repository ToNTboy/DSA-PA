include <cstdio>

int main() {
    // accept input
    int n, k;
    int *A, *B, *C;
    scanf("%d", &n);
    A = new int[n];
    B = new int[n];
    C = new int[n];
    for (int i = 0; i < n; i++) {
        scanf("%d", A[i]);
    }
    for (int i = 0; i < n; i++) {
        scanf("%d", B[i]);
    }
    for (int i = 0; i < n; i++) {
        scanf("%d", C[i]);
    }
    scanf("%d", &k);

    // judge
    
    return 0;
}
